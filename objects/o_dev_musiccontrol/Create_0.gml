channels = 4

playing = array_create(channels, -1)
playing_id = array_create(channels, -1)
curplaying = array_create(channels, -1)
curplaying_id = array_create(channels, -1)

gain = array_create(channels, 1)
pitch = array_create(channels, 1)
loop = array_create(channels, 1)

slot = 0